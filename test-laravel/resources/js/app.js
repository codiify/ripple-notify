import './bootstrap';

import RippleNotify  from 'ripple-notify';
import { RippleConfig } from '../../../ripple.js'; // Adjust the relative path as needed



const ripple = RippleNotify;

window.ripple = ripple;
